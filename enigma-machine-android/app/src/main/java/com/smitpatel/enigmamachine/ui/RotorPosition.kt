package com.smitpatel.enigmamachine.ui

/**
 * Enum class to represent which of the rotors was selected:
 * ONE (Left rotor)
 * TWO (Middle Rotor)
 * THREE (Right rotor)
 */
enum class RotorPosition {
    ONE,
    TWO,
    THREE,
}